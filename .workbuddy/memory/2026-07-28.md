# 2026-07-28

- 完成 Claude Code 风格 Skill 运行时扩展接线：`model` 经 alias 和 availableModels 校验后作为当前请求内覆盖，由 AI SDK `prepareStep.model` 切换真实模型；`effort` 通过 `providerOptions.openai.reasoningEffort` 覆盖，支持 low/medium/high/xhigh/max。
- Skill schema/loader 新增 `agent` 与 `background`。`context: fork` 复用共享子代理执行入口，Skill body 作为 prompt，清空父消息历史，支持指定 Agent 和 Skill 模型优先级。
- 当前缺少持久后台 run handle，因此 fork 仅支持 `background: false` 同步执行；后台请求明确拒绝，避免不可靠 fire-and-forget。
- 新增模型、effort、alias、白名单、inherit、fork、agent 和 background 专项测试，并更新 create-skill 指引与分析文档。
- 确定内置 `create-skill` 的唯一手写真源为 `packages/core/src/skills-builtin/create-skill/SKILL.md`；`modules/skills/bundled.ts` 改为生成产物。新增生成脚本、`generate:skills`/`check:skills-generated` 命令及一致性测试，Core test/typecheck 会阻止源文件与产物漂移。
- 核查 app 聊天用户输入框：固定有附件、模型、审批模式 3 个下拉入口；存在已启用自定义 Agent 时额外显示 Agent 下拉，共最多 4 个。附件固定 2 项、审批模式固定 3 项；模型和 Agent 项数由运行时配置/API 数据决定。对话页输入 `/` 还会打开独立的动态命令菜单。
- 明确输入框模型选择器设计方向：保留真实模型名显示，通过最大宽度、截断与 tooltip 控制空间；未来模型配置将支持多个供应商，输入框届时需要支持跨供应商模型选择，本阶段暂不实现。
- 实施输入框下拉选择器重构：触发器不再显示辅助说明（审批模式/Auto 的中文副标签通过 `data-select-secondary` 在触发器隐藏、菜单保留），Model/Agent/Mode 触发器加 `min-w-0 + max-w-* + truncate` 限制宽度；模型触发器新增 `title` 显示完整模型名。重构未引入新类型错误（`route.ts`/`reasoning.tsx` 的 15 个 TS 错误为重构前已存在），已提交 8b0fbd2。
- 进一步精简输入框下拉选项内容：Agent Auto 和模型选项删除副标题；审批模式选项改为单标签中文（默认权限/帮我审批/全部授权），删除两段式 label+hint 设计。提交 237ab93。
- 实施上下文压缩减负重构全部 4 个 Phase，全量 158 个测试通过：
  - Phase 1：代码重组 — 拆分 message-compressor.ts → force-truncate.ts + deterministic-compressor.ts，更新所有 import 路径，删除旧文件
  - Phase 2：Meta 消息 TTL 老化 — types.ts 扩展 _compactedAt，message-view.ts 扩展 CompactionPatch/ToolResultItemView，lifecycle.ts 新增 TTL_FULL_AGE=20/TTL_STUB_AGE=40 三级老化（保持→占位符→移除），ContextLedger 新增 recordEviction
  - Phase 3：Checkpoint 步数触发 — CHECKPOINT_KEEP_PERCENT 从 0.3 提升到 0.5，新增 CHECKPOINT_STEP_THRESHOLD=20/CHECKPOINT_COMPACTION_THRESHOLD=3，未提供 stepCount 时自动从消息数估算，FinalizeAgentRunOptions.checkpoint 扩展 stepCount/compactionCount 可选字段
  - Phase 4：UI 可见性 — CompactionTelemetry 新增 CompactionUINotification 类型和 onCompactionUI/notifyUI 方法，lifecycle.ts 在 manageCompaction(Layer 2 完成时)和 applyEmergencyCompression(确定性摘要/LLM 摘要/强制截断完成时)推送 UI 通知，messagesAffected <= 0 时不推送

- **BUG 修复**：TTL 跨调用计数器 `compactedAtCounter` 是 `manageToolOutputLifecycle` 局部变量，每次调用重置为 0，导致 20/40 步阈值永远达不到。修复：改为 `LifecycleOptions.compactionStep: { current: number }`（会话级对象引用），通过 `CompactionContext` → `compactBeforeStep` → `SessionState.compactionStepCounter` 全链路透传。SessionState 创建时初始化为 `{ current: 0 }`。199 测试全通过。
- 确认前端已有上下文用量指示器：Chat.tsx:1434-1458 的圆形进度环，三色（蓝≤60% / 黄>60% / 红>80%），数据来自 conversations 表的 context_usage。Phase 4 计划中的指示器无需重复实施。

- **技能创建可靠性问题排查与方案设计**：
  - 发现会话 kLszDIXAwspC0EOteMaqb 中创建的 douyin-video-downloader 技能 SKILL.md 缺少 YAML frontmatter，导致 multi-source-loader 解析时 SkillFrontmatterSchema 校验失败 → ParseError 被 catch → logger.warn 静默跳过 → / 面板不可见
  - 根因：AI 未触发 create-skill 技能，直接手写文件，遗漏了 name/description frontmatter
  - 即时修复：为 douyin-video-downloader/SKILL.md 补充了完整的 YAML frontmatter
  - 深度分析：对比了 WorkBuddy 技能创建设计（init_skill.py 脚本模板 + quick_validate.py 校验）与 TheThing 当前设计的差异
  - 输出实施文档：docs/skill-creation-reliability-plan.md，含四层优化方案（脚本化模板生成 / 系统提示强化 / 加载器错误可见化 / 写入时校验）
  - **Layer 1+2 实施完成**（脚本模板 + 触发强化）：
    - 新增 `packages/core/src/skills-builtin/create-skill/scripts/init-skill.sh`：确定性地生成带完整 YAML frontmatter 的 SKILL.md 模板。修复了 YAML 中 `[TODO: ...]` 被解析为数组的问题（改成 `"TODO: ..."` 双引号字符串）
    - 更新 `SKILL.md`：frontmatter 加 `whenToUse` 中文触发短语；Step 1 改为强制运行 init-skill.sh 脚本；Step 4 强调勿重写整个文件
    - 更新 `generate-bundled-skills.mjs`：parse 函数提取 `whenToUse`，generate 函数条件输出。验证了 `--check` 一致性通过
    - 重新生成 `bundled.ts`
    - 更新 `builder.ts`：skillCreationNote 升级为 CRITICAL 级指令（禁止直接手写 SKILL.md + 解释静默丢弃后果）
  - **技能执行死循环分析与修复**（会话 ubROqYU2wOCcuhFZ2DJa0）：
    - 分析发现 AI 使用 douyin-video-downloader 技能时陷入 4 次重复调用 skill 工具的死循环，根本原因：(1) 技能内容是"人类手册"风格（`douyin-video-downloader --link "..."`），不是 AI 可执行指令；(2) AI 混淆了"加载技能"和"执行技能指令"
    - **系统性改进 (Layer 5)**：
      - init-skill.sh 模板增强：Instructions 部分加入 AI 可执行性规范（祈使句、完整路径、环境搭建、指定工具）
      - create-skill SKILL.md 新增 Section 4 "Write AI-executable instructions"：含 5 条编写规则 + 3 条反面模式 + 好坏对比示例
      - skill.ts formatSkillOutput 尾部引导升级：从 "MUST follow instructions" 改为明确的三条执行指引（Use bash/write_file tools to EXECUTE each step / Do NOT call skill tool again）
      - 42 个相关测试全部通过
  - **技能与 Wiki 混淆问题分析与修复**（会话 Qy6fvpud → hJmgEn6）：
    - 发现 "学习" 关键字触发 AI 同时创建 skill 和 save_wiki，同一内容存两份；后续会话中 AI 用 /skill 时先去查 wiki 而非直接用 skill 工具
    - 根因：save_wiki 描述 "从外部来源获取信息后，必须保存到知识库" 的强制性语言，且未区分知识存储和配置注册
    - **修复 (Layer 6)**：
      - save-wiki.ts description 重写：加入 "Wiki 不是配置注册表" 边界声明，明确技能/MCP/connector 不属于 Wiki，附正反面示例
      - wiki-prompt.ts WIKI_GUIDELINES_PROMPT 更新：加入边界规则，"必须保存" 改为 "将整理的知识保存"
      - 全量 706 测试通过
