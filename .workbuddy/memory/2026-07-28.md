# 2026-07-28

- 完成 Claude Code 风格 Skill 运行时扩展接线：`model` 经 alias 和 availableModels 校验后作为当前请求内覆盖，由 AI SDK `prepareStep.model` 切换真实模型；`effort` 通过 `providerOptions.openai.reasoningEffort` 覆盖，支持 low/medium/high/xhigh/max。
- Skill schema/loader 新增 `agent` 与 `background`。`context: fork` 复用共享子代理执行入口，Skill body 作为 prompt，清空父消息历史，支持指定 Agent 和 Skill 模型优先级。
- 当前缺少持久后台 run handle，因此 fork 仅支持 `background: false` 同步执行；后台请求明确拒绝，避免不可靠 fire-and-forget。
- 新增模型、effort、alias、白名单、inherit、fork、agent 和 background 专项测试，并更新 create-skill 指引与分析文档。
- 确定内置 `create-skill` 的唯一手写真源为 `packages/core/src/skills-builtin/create-skill/SKILL.md`；`modules/skills/bundled.ts` 改为生成产物。新增生成脚本、`generate:skills`/`check:skills-generated` 命令及一致性测试，Core test/typecheck 会阻止源文件与产物漂移。
- 核查 app 聊天用户输入框：固定有附件、模型、审批模式 3 个下拉入口；存在已启用自定义 Agent 时额外显示 Agent 下拉，共最多 4 个。附件固定 2 项、审批模式固定 3 项；模型和 Agent 项数由运行时配置/API 数据决定。对话页输入 `/` 还会打开独立的动态命令菜单。
- 明确输入框模型选择器设计方向：保留真实模型名显示，通过最大宽度、截断与 tooltip 控制空间；未来模型配置将支持多个供应商，输入框届时需要支持跨供应商模型选择，本阶段暂不实现。
- 实施输入框下拉选择器重构：触发器不再显示辅助说明（审批模式/Auto 的中文副标签通过 `data-select-secondary` 在触发器隐藏、菜单保留），Model/Agent/Mode 触发器加 `min-w-0 + max-w-* + truncate` 限制宽度；模型触发器新增 `title` 显示完整模型名。重构未引入新类型错误（`route.ts`/`reasoning.tsx` 的 15 个 TS 错误为重构前已存在），已提交 8b0fbd2。
- 进一步精简输入框下拉选项内容：Agent Auto 和模型选项删除副标题；审批模式选项改为单标签中文（默认权限/帮我审批/全部授权），删除两段式 label+hint 设计。提交 237ab93。
