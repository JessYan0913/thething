# 2026-07-31

## Wiki 第三阶段：来源关系与修订历史

- 输出第三阶段实施方案 `outputs/wiki-revision-and-source-index-implementation-plan.md`，明确 Wiki 核心不依赖 Git，Git 仅作为未来可选导出/协作适配器。
- 提交前两阶段 Wiki 复利闭环与设计审计，提交为 `8b6cb57 feat(wiki): add compounding knowledge workflows`。
- 新增内部 Revision Store：普通页面变化保存不可变完整 Markdown 修订，支持修订列表、读取、行级 unified diff 和显式 restore；restore 形成新修订，不删除后续历史。
- 新增可重建的来源→页面反向索引 `system/source-pages.json`，支持按来源描述或稳定 source ID 查询受影响页面。
- `save_wiki` 成功写入后记录 revision 并重建来源关系；新增 `inspect_wiki_history` 与 `restore_wiki_revision` Agent 工具，恢复后同步 index、关系索引与 log，Lint 不自动触发恢复。
- 验证：9 个 Wiki 定向测试文件、27 项测试通过，Core typecheck 和 `git diff --check` 通过。

## Wiki 第三阶段安全收口

- 普通 Wiki 页面和 index 改为临时文件 + rename 原子写入；`save_wiki` 与 restore 加 Wiki 目录级进程内写锁。
- 旧页面在首次写入链路中幂等初始化 baseline revision；merge 删除来源页面前保存 delete revision，保证删除后仍可读取历史。
- 补齐 create/update/replace/invalidate revision、merge 删除历史、并发写入和 restore 来源关系测试。
- 修正两项与已回退严格 Skill 治理不一致的旧测试断言，不恢复精确工具证据验收。
- Core 完整测试结果：80 个文件、757 项通过；剩余 2 项既有失败为真实 DB 抽样为空，以及对话路线主分支名称实际为 null 但测试期待“主分支”，均与 Wiki 改动无关。Core typecheck 通过。

## 清空 Wiki 并准备真实回归

- 用户明确确认后，将 `~/.thething/wiki/` 的 19 个现有文件先完整备份到 `/Users/yanheng/.thething/wiki-backups/wiki-20260731-090001`，通过 `diff -rq` 验证一致，备份保留 20 个目录条目。
- Finder 自动化因 macOS 权限拒绝，确认未发生部分移动后，改用系统 `/usr/bin/trash`，按 10 + 9 两批移入废纸篓；最终 Wiki 目录为空且保留。
- 输出从空库开始的真实回归指南 `outputs/wiki-third-stage-feature-test-guide.md`，覆盖来源级 Ingest、来源去重、revision、diff、来源反向索引、restore、Query 可选回写、Lint、批量 Action 和重启持久化。

## Wiki 第三阶段真实对话审计

- 审计对话 `jluFC2O1glCPUx-sv10Ef`，从 head message 沿 parent 链重建 26 条唯一活跃消息，排除 sibling 分支后对照 A～L 场景核验工具调用和磁盘产物。
- 输出 `outputs/wiki-third-stage-real-conversation-audit-2026-07-31.md`；判定为 5 项通过、4 项部分通过、2 项失败、1 项未执行，整体尚未通过。
- 底层 URL 来源去重、不可变快照、revision parent 链、diff、显式 restore、restore 后更新和非破坏性 Lint 均真实生效。
- 最高优先级问题：Agent 可用通用文件编辑工具直接修改 `~/.thething/wiki/*.md`，导致当前主页面与最新 revision 哈希不一致；其次是 `ingest_wiki_source` 不允许空 actions、页面 target 规范化导致双 revision 链、Git 来源未按测试要求登记，以及 Query 回写未显式记录 `origin: query`。

## Wiki 真实回归问题修复

- 将整个 Wiki 根目录设为受管写入路径：`write_file`、`edit_file` 和具备写能力且引用 Wiki 路径的 Bash 命令会被拒绝，只读 Bash 命令仍允许；写入必须通过 Wiki 专用 mutation 工具以维护 revision、index、来源关系和 log 一致性。
- `ingest_wiki_source` 支持 `actions: []`，可只登记、保存快照或去重来源，不制造无价值页面修订。
- 新增统一 Wiki 页面 resolver，save/read/history/diff/restore 可将自然语言名称、空格文件名、kebab 文件名解析到同一真实页面；同义 create 明确失败，避免双页面与双 revision 链。
- 强化 Wiki 指南：Query 中新综合回写必须显式 `origin: query`；用户明确给出的 Git type/value/revision 必须按原语义登记，不得降级为 conversation 来源。
- 验证：Wiki 定向 11 个测试文件、51 项测试通过；Core typecheck 与 `git diff --check` 通过；Core 完整测试 82/83 个文件、773/774 项通过，唯一失败仍为既有 `branch-store.test.ts` 主分支名称期待值不一致，与本次 Wiki 修复无关。

## 第二轮真实回归清场

- 用户明确确认后，将第二轮回归前的 `~/.thething/wiki/` 27 个文件（约 108 KB）完整备份到 `/Users/yanheng/.thething/wiki-backups/wiki-before-second-regression-20260731-103959`，并通过 `diff -rq` 验证备份与原目录一致。
- 27 个 Wiki 文件按 10 + 10 + 7 三批移入 macOS 废纸篓，每批均验证成功；随后将空的 `raw/` 和 `system/` 子目录移入废纸篓，没有永久删除。
- 保留 `/Users/yanheng/.thething/wiki/` 根目录，最终确认目录完全为空；未触碰聊天数据库或其他 TheThing 数据，可开始第二轮 A～L 真实回归。
- 输出第二轮专用测试话术 `outputs/wiki-second-round-regression-test-prompts-2026-07-31.md`，覆盖零 Action 去重、canonical target、Git 来源、重复来源查询、restore 来源回退、Query origin、Lint 工具结果、多 Action、通用 edit/Shell 写入拦截、只读放行和真正重启持久化，并列出整体通过标准与审计证据。
- 第二轮测试来源改为真实公开仓库 `https://github.com/anthropics/skills`，固定版本 `b29e7cf65e5cb78a5ac33d582270551bc74a14eb`；URL 来源使用该 commit 下的 README 地址，Git 来源使用仓库 URL 与同一 commit，确保重复摄取和来源关系测试可复现。
- 第二轮 A～K 测试对话地址为 `http://localhost:3000/chat/user/rf4Te9h_AYsHVl21-J5Hs`，conversation ID 为 `rf4Te9h_AYsHVl21-J5Hs`；后续审计需从该对话 head 沿 parent 链重建活跃分支，并与真正重启后的 L 场景对话分开核验。

## Wiki 第二轮真实回归审计

- 从第二轮 conversation 的 head 沿 parent 链重建 34 条唯一活跃消息；数据库总数同为 34，无 sibling 分支污染。
- 输出正式报告 `outputs/wiki-second-round-real-conversation-audit-2026-07-31.md`：B、D、E、F、G、H、J 共 7 项通过；A、C、I、K 共 4 项部分通过；L 因缺真正重启外部证据判为证据不足，整轮尚不能宣布完整通过。
- 核心修复已真实生效：零 Action 去重、canonical resolver、Git 来源语义、restore 新修订及来源回退、Query origin、Shell 受管路径拒绝。
- 当前 3 个页面与各自最新 revision 哈希完全一致；主页面 8 个 revision parent 链连续；URL snapshot 哈希与 registry 一致；无临时文件或拒绝测试文本残留。
- 待补最关键证据：K2 用绝对 Wiki 路径直接命中 managed edit guard；L 提供 K 与 L 之间真正退出并重启 TheThing 的外部证据。
