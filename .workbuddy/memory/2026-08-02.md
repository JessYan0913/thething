# 2026-08-02

- 修复对话消息渲染回归：在 `packages/app/components/ai-elements/message.tsx` 的 assistant `MessageContent` 恢复克制的左侧视觉锚，采用 `border-l border-primary/15 pl-3`，使 assistant 文字与夹在其中的工具调用保持清晰的回合分组；未扩展到 `ask_user_question` 定制回显。
- 排查对话 `Z7waH_TYWGrDMwN-ffQxH` 的 `ask_user_question`：回答已写入工具审批响应的 `reason`（`{"answers":{"神恶魔":"我是说你能做什么"}}`），但对话持久化结果只有 `approval-responded`，没有工具 output 或后续 assistant 文本，说明自动续跑/最终回复链路未完成；另外首次工具调用因 `questions` 被模型输出为字符串而 `output-error`。
- 修复 `ask_user_question` 审批续跑持久化：新增 `commitAssistantContinuation`，将 `approval-requested → approval-responded → output-available` 作为同一 assistant 工具消息的不可变版本推进；`/api/chat` 不再把审批续跑 assistant 当 user 提交，并在 onEnd 保存工具 output 后把最终文本锚定其后。
- 根据真实报错修正续跑关联规则：流式客户端 assistant id（如 `ZTqCW...`）与服务端持久化 head id（如 `HAiif...`）并不稳定一致，不能按消息 id 关联。现改为只匹配当前 assistant head 上相同 `toolCallId` 的合法状态迁移（requested→responded、responded→output/error），同时拒绝 stale head、不同工具调用和非法状态跃迁；message-store 32/32 测试、core/app typecheck 均通过。
- 修复 `ask_user_question` 工具输入 schema 错误被吞：新增 `refineAskUserQuestionInput`（`packages/core/src/modules/tools/ask-user-question.ts`），在 ToolLoopAgent 上接入 `experimental_refineToolInput`，把模型偶发把 `questions` 序列化为 JSON 字符串字面量在 schema 校验前 parse 回数组；新增 5 个单测覆盖正常/异常路径，core 全量 822/822 通过。
- 修复 Chat.tsx 失败工具调用不显示 errorText：在 `packages/app/components/Chat.tsx` tool-<name> 渲染处提取 `toolPart.errorText`，并在 bashStreamPart 卡片之后追加一个红底错误展示 div，让用户和 Agent 都能看到失败原因；app typecheck 通过。
