# 2026-08-05 工作日志

## 修复上下文详情数据展示错误

**根因**：prepareStep 估算（FullRequestEstimation）有正确的当前窗口占用 + 明细字段，但 pipeline 从未写入 `sessionState.lastEstimation`，导致 route.ts onStepEnd 推送时只能从 TokenBudgetTracker 取累计值冒充窗口占用，且明细字段完全缺失。

**改动文件**（共 7 处，5 个文件）：
1. `session/types.ts:132` — lastEstimation 类型从 CachedEstimation 改为 FullRequestEstimation（前者无 outputReserve）
2. `session/interfaces.ts` — PipelineContext 加 lastEstimation 字段 + import FullRequestEstimation
3. `agent-control/pipeline.ts:212-214` — 写入 sessionState.lastEstimation = estimation + 更新过时注释
4. `app/api/chat/route.ts` — 主分支 onStepEnd 推送 (483-502) 补 messagesTokens/instructionsTokens/toolsTokens/outputReserve，百分比和总计优先用估算值
5. `app/api/chat/route.ts` — 主分支 updateContextBudget 写库 (507-522) 补明细字段
6. `app/api/chat/route.ts` — 重试分支 onStepEnd 推送 (557-577) 同步改
7. `app/api/chat/route.ts` — 重试分支 updateContextBudget 写库 (579-592) 同步改

**前端优化**：
- Chat.tsx ContextBarItem 加百分比显示（token 值 + 占比%），进度条宽度优化
- Chat.tsx ChatProps.contextBudget 类型补 compactionTriggerWatermark 字段

**类型检查**：core 包零错误，app 包仅剩 7 个既有的 previewData null 警告。

## 前端 UI 优化：上下文用量弹窗重构

- 弹窗标题「上下文占用详情」→「上下文用量」
- 总计区块：大字号百分比 + h-2.5 粗进度条 + 刻度标注（0%/当前%/100%），去掉"总计"文字标签
- 明细区块：加"明细"标签，四个条目带百分比
- 「会话累计」→「会话消耗」
