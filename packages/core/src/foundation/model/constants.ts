// ============================================================
// Model Constants - 模型能力默认值
// ============================================================
// 这些是模型固有的技术常量，不是用户业务决策。
// 调用方可通过 BehaviorConfig.maxContextTokens 覆盖。
// ============================================================

/** 默认上下文限制（保守估计） */
export const DEFAULT_CONTEXT_LIMIT = 128_000;

/** 默认输出预留 */
export const DEFAULT_OUTPUT_TOKENS = 8_000;
