{
  "name": "@the-thing/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "build/main.js",
  "scripts": {
    "dev": "electron .",
    "build": "tsc -p tsconfig.json && electron-builder",
    "prepare:standalone": "tsx scripts/prepare-standalone.ts",
    "prepare:node": "tsx scripts/prepare-node-runtime.ts",
    "rebuild:sqlite": "electron-rebuild -f -w better-sqlite3 -m ../app"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.0",
    "@types/node": "^20",
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "tsx": "^4.19.3",
    "typescript": "^5"
  }
}
